<!DOCTYPE html>
<html lang="en" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= brand.name %> · First run</title>
    <link rel="icon" type="image/png" sizes="32x32" href="/brand/icon-32.png" />
    <link rel="apple-touch-icon" href="/brand/apple-touch-icon.png" />
    <link rel="stylesheet" href="/dashboard.css" />
  </head>
  <body class="gateway-page">
    <main class="gateway-shell">
      <section class="gateway-story" aria-label="About <%= brand.name %>">
        <div class="brand-lockup" aria-label="<%= brand.name %>">
          <img src="/brand/mark.svg" alt="" />
          <span><strong><%= brand.name %></strong><small><%= brand.tagline %></small></span>
        </div>

        <div class="gateway-copy">
          <span class="eyebrow">Your first minute</span>
          <h2>Make the workspace yours.</h2>
          <p>Choose one strong password, then manage the assistant from a single private control room.</p>
        </div>

        <div class="gateway-mark" aria-hidden="true">
          <span class="gateway-ring"></span>
          <img src="/brand/mark.svg" alt="" />
        </div>

        <p class="gateway-trust"><span></span> One setup · no configuration files</p>
      </section>

      <section class="gateway-panel">
        <div class="gateway-panel-inner">
          <form class="card auth-card setup-card" action="/setup" method="post">
            <div class="auth-head">
              <span class="eyebrow">Create your workspace</span>
              <h1>Welcome to <%= brand.name %></h1>
              <p class="sub">Protect the linked WhatsApp account with at least <%= minLength %> characters.</p>
            </div>

            <% if (needsCode) { %>
            <label class="field">
              <span>Setup code</span>
              <input type="text" name="code" autocomplete="off" autofocus required
                     placeholder="Printed in the bot's terminal" />
              <small>Confirms you can access the server running Levix.</small>
            </label>
            <% } %>

            <label class="field">
              <span>Password</span>
              <input type="password" name="password" autocomplete="new-password"
                     placeholder="At least <%= minLength %> characters"
                     minlength="<%= minLength %>" <%= needsCode ? "" : "autofocus" %> required />
            </label>

            <label class="field">
              <span>Repeat password</span>
              <input type="password" name="confirm" autocomplete="new-password"
                     placeholder="Type it again" minlength="<%= minLength %>" required />
            </label>

            <button type="submit" class="btn btn-primary auth-submit">
              Create workspace
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            </button>

            <% if (error) { %>
            <div class="error" role="alert"><%= error %></div>
            <% } %>
          </form>

          <div class="foot"><%= brand.name %> by <%= brand.developer %> · <%= brand.studio %></div>
        </div>
      </section>
    </main>
  </body>
</html>
